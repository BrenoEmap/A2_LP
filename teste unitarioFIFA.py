import unittest
from math import pi
from datapolishFIFA import df

class TesteFIFA(unittest.Testcase):
    def test_thingy(self):
        
        self.assertEqual(df,data_fifa)
        self.assertEqual(prob_acima,len(acima)/total)
        self.assertEqual(prob_igual,len(igual)/total)
        self.assertEqual(prob_abaixo,len(abaixo)/total)
        self.assertEqual(total,len(acima) + len(abaixo) + len(igual))

        
   
